<UserControl x:Class="YMM4.GeminiTTS.Plugin.UI.AudioTagToolView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:YMM4.GeminiTTS.Plugin.UI"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=local:AudioTagToolViewModel}"
             FocusManager.IsFocusScope="False"
             Focusable="False"
             MinWidth="180">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="6">
            <TextBlock Text="セリフ欄にカーソルを置いてからボタンを押すと挿入されます。"
                       TextWrapping="Wrap" FontSize="11" Opacity="0.6" Margin="0,0,0,6"/>

            <ItemsControl ItemsSource="{Binding Groups}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type local:TagGroup}">
                        <Expander Header="{Binding Header}" IsExpanded="True" Margin="0,0,0,4">
                            <ItemsControl ItemsSource="{Binding Items}" Margin="4,4,0,4">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate DataType="{x:Type local:TagItem}">
                                        <Button Content="{Binding Label}"
                                                ToolTip="{Binding ToolTip}"
                                                Command="{Binding DataContext.InsertTagCommand,
                                                    RelativeSource={RelativeSource AncestorType=ItemsControl, AncestorLevel=2}}"
                                                CommandParameter="{Binding Tag}"
                                                Focusable="False"
                                                Padding="7,3" Margin="2"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Expander>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
